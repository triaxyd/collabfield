<ul class="navbar-nav ms-auto">
  <% if user_signed_in? %>
    <li class="nav-item dropdown">
      <a id="user-settings" class="nav-link dropdown-toggle text-white" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
        <span id="user-name"><%= current_user.name %></span>
      </a>
      <ul class="dropdown-menu">
        <li><%= link_to 'Edit Profile', edit_user_registration_path, class: 'dropdown-item text-white' %></li>
        <li><%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'dropdown-item text-white' %></li>
      </ul>
    </li>
  <% else %>
    <li class="nav-item"><%= link_to 'Login', login_path, class: 'nav-link text-white' %></li>
    <li class="nav-item"><%= link_to 'Signup', signup_path, class: 'nav-link text-white' %></li>
  <% end %>
</ul>